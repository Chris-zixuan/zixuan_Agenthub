# 项目上下文 — <项目名>

> 本文件是项目专属的**唯一真相源**。所有变量定义、通信参数、外部程序信息都记录在此。
> 基线 Skill 在生成代码前会读取本文件。

---

## 一、外部通信

<!-- 如无外部通信需求，整节删除 -->

| 项目 | 值 |
|------|-----|
| 通信方式 | `<NamedPipe / TCP Client / TCP Server / 串口 / 文件监控 / 其他>` |
| 对端程序 | `<外部程序名.exe，语言，位数>` |
| 连接参数 | `<管道名 / IP:Port / 串口号+波特率>` |
| 通信协议 | `<JSON / 自定义二进制帧 / Protobuf / 纯文本>` |
| 帧格式 | `<描述，如：4 字节头 (小端长度) + JSON body>` |
| 超时设置 | `<连接超时 ms>/<读写超时 ms>` |
| 重连策略 | `<是否自动重连，间隔>` |

## 二、固定输入变量

<!-- 根据 UserProperty.cs 填写，只列与项目逻辑相关的变量 -->

| 变量名 | VM 类型 | C# 类型 | 说明 |
|--------|---------|---------|------|
| `<in0>` | `<INT>` | `<int>` | `<说明>` |
| `<in1>` | `<IMAGE>` | `<ImageData>` | `<说明>` |

## 三、固定输出变量

| 变量名 | VM 类型 | C# 类型 | 说明 |
|--------|---------|---------|------|
| `<out0>` | `<INT>` | `<int>` | `<说明>` |
| `<out1>` | `<IMAGE>` | `<ImageData>` | `<说明>` |

## 四、全局变量

<!-- 如无全局变量，整节删除 -->

| 变量名 | 类型 | 读写 | 说明 |
|--------|------|------|------|
| `<var_name>` | `<STRING>` | `<读取/写入/读写>` | `<说明>` |

## 五、模块参数

<!-- 如不需要读写其他模块参数，整节删除 -->

| 模块名 | 参数名 | 读写 | 说明 |
|--------|--------|------|------|
| `<模块名>` | `<参数名>` | `<读取/写入>` | `<说明>` |

## 六、特殊约定

<!-- 任何基线规则之外的特殊处理，例如： -->
<!-- - Process() 末尾必须调用 LogModule.WriteLog() 写入日志 -->
<!-- - 图像输出前必须转为 8 位灰度 -->
<!-- - 异常时必须设置 result_code = -1 并 return false -->
